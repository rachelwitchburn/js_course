'use strict';  // deve ser o primeiro script do código inteiro
// strict fala onde erramos

let hasDriversLiscense = false;
const passTest = true;

if (passTest) hasDriverLiscense = true; // ele avisará que está escrito de maneira errada
if (hasDriversLiscense) console.log(`I can drive!`);

// const interface = 'Audio';  // mostra que a palavra 'interface' é reservada para js
const private = 534;  // também reserva a palavra